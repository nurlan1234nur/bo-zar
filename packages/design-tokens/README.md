# Design Tokens

Shared semantic light/dark theme values used by BO Zar clients.

The package defines colors, spacing, radius, layout, and typography tokens. Cross-client visual changes should update semantic tokens first; component-specific behavior and styling remain within each application.

See the canonical [design system](../../docs/design/design-system.md).
