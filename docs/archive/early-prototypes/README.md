# Archived early prototypes

The `marketplace-platform` directory is a standalone Figma-exported Vite prototype preserved with its source, configuration, guidelines, and attribution.

It is not part of the BO Zar runtime workspace and must not be treated as production functionality.
